[project]
name = "agents-owl"
version = "0.2.0"
description = "Terminal-first native session management and optional worker/peer collaboration"
requires-python = ">=3.10"
license = "MIT"

[tool.ruff]
line-length = 120

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
